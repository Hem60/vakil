__pycache__/
*.py[cod]
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
dist/
build/
*.egg-info/
node_modules/
.next/
data/train/*.json
data/test/*.json
!data/test/MANIFEST.json
# Rendered POD documents: 25 MB, regenerable from seed by `make fixtures`.
# The manifest carries the ground truth and is committed.
data/fixtures/pod/
ledger.jsonl
ledger.db
*.log
"screen short"/
# Extraction checkpoints: resumable run state, not a result.
evals/.extraction_*.jsonl

# Personal pitch material, kept out of the repo deliberately. A script
# telling a panel how to answer their own questions reads badly to that
# panel. It was committed once by mistake and purged from history.
docs/PITCH.md
